name: aws
type: cloud

depends_on:
  languages:
    - python

paths:
  - ai/domains/aws.md
  - ai/skills/aws/

dependencies:
  extras:
    - cloud
  groups:
    - dev-cloud

artifacts:
  - context_bundle
